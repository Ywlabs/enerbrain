# 앱
APP_NAME=EnerBrain API
API_V1_PREFIX=/api/v1

# PostgreSQL (미설정 시 DB 세션 의존 기능은 사용하지 않음)
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/enerbrain

# CORS — JSON 배열 문자열
CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
